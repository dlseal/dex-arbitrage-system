# --- 核心异步网络框架 ---
aiohttp>=3.9.0
fastapi>=0.100.0
uvicorn[standard]>=0.20.0

# --- 交易所 SDK ---
# GRVT 官方 SDK
grvt-pysdk>=0.2.1

# Lighter.xyz (指定从 GitHub 安装)
git+https://github.com/elliottech/lighter-python.git#egg=lighter-python

# --- 配置管理 (新增) ---
pydantic-settings>=2.0.0  # 用于 BaseSettings
PyYAML>=6.0               # 用于解析 config.yaml

# --- 通用依赖 ---
ccxt>=4.0.0
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.24.0
web3>=7.0.0
eth-account>=0.13.4
pydantic>=2.0.0